{
  "name": "@modbender/game-kit",
  "version": "0.0.0",
  "private": true,
  "description": "Host adapters and the shared layer behind small web games.",
  "type": "module",
  "license": "MIT",
  "files": ["dist"],
  "exports": {
    "./log": { "types": "./dist/log.d.ts", "import": "./dist/log.js" },
    "./rng": { "types": "./dist/rng.d.ts", "import": "./dist/rng.js" }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^5.6.3"
  }
}
